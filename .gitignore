gigo-food/backend/node_modules
gigo-food/frontend/node_modules
node_modules/
.env
dist/
.DS_Store

# Ignore future runtime-uploaded images (admin panel adds), but keep the
# 32 seed images tracked so `node seed/seedFoods.js` works on every deploy.
backend/uploads/*
!backend/uploads/.gitkeep
!backend/uploads/food_*.png
