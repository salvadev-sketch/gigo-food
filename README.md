# GIGO Food

Food delivery web app — customer site + admin panel.

## Status: Backend live, frontends built, deployment in progress
- ✅ Backend (Node/Express + MongoDB) deployed and live at `https://gigo-food.onrender.com`
- ✅ Customer frontend (React/Vite) built and build-verified, not yet deployed
- ✅ Admin panel (React/Vite, separate app) built and build-verified, not yet deployed
- 🔲 Menu seeding not yet run — see `PROJECT_STATUS.md`
- 🔲 MTN MoMo sandbox credentials not yet configured
- 🔲 Customer + admin frontends not yet deployed to Vercel

See `PROJECT_STATUS.md` for the full phase-by-phase breakdown and next steps.

## Stack
- Frontend: React + Vite (customer site + separate admin app)
- Backend: Node/Express + MongoDB (shared Atlas cluster)
- Auth: JWT + bcrypt
- Payments: MTN MoMo (Collections API)
- Currency: FRw
