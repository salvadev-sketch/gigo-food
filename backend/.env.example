# MongoDB (same cluster as other GIGO projects, own db name)
MONGO_URI=mongodb+srv://tuyisengeemmanuel526_db_user:<password>@cluster0.aerbtfv.mongodb.net/gigo-food?retryWrites=true&w=majority

# Auth
JWT_SECRET=replace_with_a_long_random_string

# Frontend URLs (for CORS + email links)
FRONTEND_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174

# Server
PORT=4000

# Email (forgot/reset password - matches gigo-pharmacy/gigo-delivery pattern)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=

# --- MTN MoMo (Collections API) ---
# Get these from the MTN MoMo Developer Portal: https://momodeveloper.mtn.com
# Start in sandbox (MOMO_TARGET_ENV=sandbox) then switch to production once approved.
MOMO_SUBSCRIPTION_KEY=
MOMO_API_USER=
MOMO_API_KEY=
MOMO_TARGET_ENV=sandbox
MOMO_BASE_URL=https://sandbox.momodeveloper.mtn.com
MOMO_CALLBACK_URL=https://your-backend.onrender.com/api/order/momo-callback
